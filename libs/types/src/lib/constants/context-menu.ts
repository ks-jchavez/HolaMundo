import { ContextMenuSection } from '../enums';

export const ContextMenuDefaultSectionOrder = [
  ContextMenuSection.CrossLink,
  ContextMenuSection.Filter,
  ContextMenuSection.Preview,
  ContextMenuSection.Investigation,
];

export const ContextMenuInvestigationSectionOrder = [ContextMenuSection.Investigation];

export const ContextMenuPreviewSectionOrder = [ContextMenuSection.Investigation];
